---
# handlers file for vector-role
- name: Restart vector service
  ansible.builtin.service:
    name: vector
    state: restarted
  when:
    - ansible_service_mgr == 'systemd'
    - not (ansible_virtualization_type | default('') in ['docker', 'container', 'lxc'])
